# CurrentTime-SpringApplication

Spring boot application with one Controller class i.e TimeApplication which implements TimeApplicationInterface that contains getCurrentTime mehtod which will be called when "/status" api is called , and it will return "Running, {Current Time}"
